import { Injectable } from '@angular/core'
import { MessageViewMessageEffectsBase } from './message-view-message.effects-base'
/**
 * The basic effect of the `view` component that fetches the object's value.
 * It also handles the business logic and navigation of the configured actions using the `Button` component.
 *
 * You can override, if needed, all the generated methods & variables of the Base effect in this class.
 *
 * @AutoSkip Do not remove this tag to keep your file out of regeneration.
 * @Note This effect is generated once and will no more be erased by the generator.
 */
@Injectable()
export class MessageViewMessageEffects extends MessageViewMessageEffectsBase {
  constructor() {
    super()
  }
}
